defaults: &defaults
  max_export_records_count: 100
  signin_url: /signin
  root_prefix: /liza
  start_year: 2021
  default_url_options: &default_url_options
    protocol: <%= ENV.fetch('HTTP_PROTOCOL', 'https') %>
    host: <%= ENV.fetch('LIZA_HOST', 'localhost') %>

  peatio_api_url: https://dapi.bitzlato.bz/api/v2/peatio
  barong_api_url: https://dapi.bitzlato.bz/api/v2/barong
development:
  <<: *defaults
staging:
  max_export_records_count: 2000
  <<: *defaults
production:
  max_export_records_count: 2000
  <<: *defaults
test:
  <<: *defaults
